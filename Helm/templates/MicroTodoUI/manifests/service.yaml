apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-todo-ui
spec:
  type: {{ .Values.todoUI.type }}
  selector:
    app: {{ .Release.Name }}-todo-ui
  ports:
    - port: {{ .Values.todoUI.port }}
      targetPort: {{ .Values.todoUI.targetPort }}
